{
  "name": "cargolane",
  "private": true,
  "engines": { "node": ">=22" },
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
